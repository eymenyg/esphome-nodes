esphome:
  name: esp01relay

esp8266:
  board: esp01_1m

# Enable logging
logger:

# Enable Home Assistant API
api:
  password: !secret api_password

ota:
  password: !secret api_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Esp01Relay Fallback Hotspot"
    password: "dUUoTMRKHchh"

captive_portal:
    

switch:
  - platform: gpio
    pin: GPIO0
    name: "Relay"
    inverted: true
